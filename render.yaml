services:
  - type: web
    name: my-cv
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn my_cv.wsgi:application
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: my_cv.settings
      - key: PYTHON_VERSION
        value: "3.12.0"
